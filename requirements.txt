# Runtime dependencies.
#
# Verified on Python 3.12.7 with the versions pinned below.  Newer minor
# releases are expected to work; these are the ones the results in README.md
# were produced with.
#
#   pip install -r requirements.txt
#
# On a machine without a CUDA GPU, install the CPU build of PyTorch first --
# it is a much smaller download and everything here runs fine on CPU:
#
#   pip install torch --index-url https://download.pytorch.org/whl/cpu
#
# The tools in tools/ (smoke_test.py, visualize_netlist.py,
# build_dataset_manifest.py) need NONE of this; they are standard library only.

torch>=2.0
torch_geometric>=2.4
scikit-learn>=1.3
numpy>=1.24

# Optional: only needed for the spreadsheet export in build_graph.py --excel,
# which is a feature-debugging aid, not part of training or inference.
pandas>=2.0
openpyxl>=3.1
