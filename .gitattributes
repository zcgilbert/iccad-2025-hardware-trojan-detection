# Normalise line endings to LF in the repository.  The synthesis flows run on
# Linux, where a CRLF shell script will not execute.
* text=auto eol=lf

# Binaries: never touch these.
*.pt binary
*.db binary
*.xlsx binary
*.pdf binary
*.png binary
*.jpg binary
