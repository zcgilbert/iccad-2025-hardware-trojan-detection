# `docs/`

| Document | What it covers |
|---|---|
| [`dataset.md`](dataset.md) | How the training corpus was built: the two synthesis flows, why labels have to be extracted before anonymisation, the rewriting rules, and what verifying them turned up |
| [`results.md`](results.md) | The official scorecard read closely: the design-level confusion matrix, why the headline mean F1 understates the result, the three failure modes, and where the missing points are |

`figures/` holds the two circuit diagrams embedded in the
[main README](../README.md#what-the-model-actually-sees). They are plain SVG —
open either one directly to hover a gate for its name.
